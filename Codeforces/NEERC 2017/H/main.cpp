#include <bits/stdc++.h>

using namespace std;

int dp[70][70];
int opt[70][70];

int main()
{
    int T, K, w, h, i, j;
    
    freopen("hard.in", "r", stdin);
    freopen("hard.out", "w", stdout);
    
    for(i = 1; i <= 60; i++)
    {
        dp[i][j] = 1;
        
        for(j = i + 1; j <= 60; j++)
        {
            
        }
    }
    
    
    
    
    return 0;
}
